include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
  errors:
    # Doc-comment coverage is a larger, dedicated effort — don't block the
    # build on it in this foundation slice.
    public_member_api_docs: ignore
