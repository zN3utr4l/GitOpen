# Shell scripts must keep LF line endings even when checked out on Windows;
# the release workflow runs them on Linux runners where CRLF breaks the
# shebang and command parsing.
*.sh text eol=lf
scripts/* text eol=lf
